import { fileDictionary } from "../data/mockData";

function loadcsv(fileName: string) {
    if (fileDictionary.get(fileName)) {
        //if it doesn't throw an error
    }
}
//make sure that there is a file with the given name within the file

//have a status code for view
function viewcsv() {
    //if load returned something we expected, allow this to print out the thing as a table
}
